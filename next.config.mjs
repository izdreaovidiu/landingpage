/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.giallozafferano.it",
      "flavorthoughts.com",
      "www.recipetineats.com",
    ],
  },
};

export default nextConfig;
